# Быстрый старт: Capstone 3

## Что было сделано

✅ Создан FastAPI бэкенд для работы с реальными моделями  
✅ Интегрирован фронтенд с API  
✅ Добавлена автоматическая проверка доступности API  
✅ Реализован fallback на симуляцию при недоступности API  

## Как использовать

### Шаг 1: Подготовка моделей

Если модели еще не обучены:

1. Откройте `notebooks/lstm_training.ipynb` в Google Colab
2. Запустите все ячейки до конца
3. Сохраните vocab в конце ноутбука:
   ```python
   import json
   import os
   os.makedirs('../vocab', exist_ok=True)
   with open('../vocab/vocab.json', 'w', encoding='utf-8') as f:
       json.dump(vocab, f, ensure_ascii=False, indent=2)
   ```
4. Скачайте `best_lstm_model.pth` и `best_cnn_model.pth`
5. Поместите их в папку `models/`

### Шаг 2: Запуск бэкенда

```bash
# Установка зависимостей (если еще не установлены)
pip install -r requirements.txt

# Запуск бэкенда
uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

Вы должны увидеть:
```
✓ LSTM model ready
✓ CNN model ready
✓ Models initialized
```

### Шаг 3: Открытие фронтенда

Откройте `docs/index.html` в браузере.

Интерфейс автоматически:
- Проверит доступность API
- Переключится на реальные модели если API доступен
- Покажет статус подключения внизу страницы

## Проверка работы

1. Откройте вкладку "🤖 Model Demo"
2. Введите текст новости или выберите пример
3. Выберите модель (LSTM, CNN или All Models)
4. Нажмите "🔍 Classify"
5. Если API работает, вы увидите "(Real Model)" рядом с результатом

## Устранение проблем

**API недоступен:**
- Убедитесь, что бэкенд запущен на порту 8000
- Проверьте консоль браузера на ошибки CORS
- Проверьте логи бэкенда

**Модели не загружаются:**
- Проверьте наличие файлов `models/best_lstm_model.pth` и `models/best_cnn_model.pth`
- Проверьте наличие `vocab/vocab.json`
- Проверьте логи бэкенда при запуске

**Предсказания не работают:**
- Убедитесь, что vocab соответствует обученным моделям
- Проверьте формат входного текста (не должен быть пустым)

## Структура файлов

```
backend/
├── main.py              # FastAPI приложение
├── models/              # Определения моделей
├── preprocessing/       # Предобработка текста
└── utils/              # Загрузка моделей

models/                  # Сохраненные модели (нужно добавить)
├── best_lstm_model.pth
└── best_cnn_model.pth

vocab/                   # Vocab (нужно добавить)
└── vocab.json

docs/
└── index.html          # Обновленный фронтенд
```

## API Endpoints

- `GET /api/health` - Проверка статуса
- `POST /api/predict/lstm` - Предсказание LSTM
- `POST /api/predict/cnn` - Предсказание CNN
- `POST /api/predict/all` - Сравнение всех моделей

Подробнее см. `README_CAPSTONE_3.md`

