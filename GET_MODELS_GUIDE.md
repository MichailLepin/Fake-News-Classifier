# Руководство по получению обученных моделей и словаря для Railway

Это руководство поможет вам получить все необходимые файлы для деплоя на Railway.

## 📋 Необходимые файлы

Для успешного деплоя на Railway вам понадобятся:

1. **`models/best_cnn_model.pth`** - обученная CNN модель
2. **`models/best_lstm_model.pth`** - обученная LSTM модель  
3. **`models/best_bert_model/`** - обученная BERT модель (папка с файлами)
4. **`models/best_distilbert_model/`** - обученная DistilBERT модель (папка с файлами)
5. **`vocab/vocab.json`** - словарь для предобработки текста (только для CNN/LSTM)

## 🚀 Способ 1: Обучение в Google Colab (Рекомендуется)

### Шаг 1: Откройте ноутбуки в Google Colab

1. Перейдите на [Google Colab](https://colab.research.google.com/)
2. Откройте файлы:
   - `notebooks/cnn_training.ipynb` - для CNN модели
   - `notebooks/lstm_training.ipynb` - для LSTM модели
   - `notebooks/bert_training.ipynb` - для BERT модели
   - `notebooks/distilbert_training.ipynb` - для DistilBERT модели

   Или используйте прямые ссылки из ноутбуков (кнопка "Open In Colab")

### Шаг 2: Запустите обучение CNN модели

1. В ноутбуке `cnn_training.ipynb` выполните все ячейки по порядку:
   - Установка зависимостей
   - Загрузка и обработка данных
   - Построение словаря
   - Загрузка GloVe embeddings
   - Обучение модели (может занять 20-30 минут)
   - Оценка на тестовом наборе

2. После обучения модель автоматически сохранится как `best_cnn_model.pth`

3. **ВАЖНО:** Выполните ячейку "Download Model for Railway Deployment" в конце ноутбука
   - Файл `best_cnn_model.pth` будет скачан в папку Downloads

### Шаг 3: Запустите обучение LSTM модели

1. В ноутбуке `lstm_training.ipynb` выполните все ячейки по порядку
2. После обучения модель сохранится как `best_lstm_model.pth`
3. Выполните ячейку "Download Model for Railway Deployment"
   - Файл `best_lstm_model.pth` будет скачан

### Шаг 3.5: Запустите обучение BERT модели

1. В ноутбуке `bert_training.ipynb` выполните все ячейки по порядку:
   - Установка зависимостей (включая transformers)
   - Загрузка и обработка данных
   - Инициализация BERT токенизатора и модели
   - Fine-tuning модели (может занять 30-60 минут на GPU)
   - Оценка на тестовом наборе

2. После обучения модель сохранится в папке `best_bert_model/`

3. **ВАЖНО:** Выполните ячейку "Download Model for Railway Deployment"
   - Файл `best_bert_model.zip` будет скачан в папку Downloads

### Шаг 3.6: Запустите обучение DistilBERT модели

1. В ноутбуке `distilbert_training.ipynb` выполните все ячейки по порядку:
   - Установка зависимостей (включая transformers)
   - Загрузка и обработка данных
   - Инициализация DistilBERT токенизатора и модели
   - Fine-tuning модели (может занять 20-40 минут на GPU, быстрее чем BERT)
   - Оценка на тестовом наборе

2. После обучения модель сохранится в папке `best_distilbert_model/`

3. **ВАЖНО:** Выполните ячейку "Download Model for Railway Deployment"
   - Файл `best_distilbert_model.zip` будет скачан в папку Downloads

### Шаг 4: Сохраните словарь (только для CNN/LSTM)

**ВАЖНО:** Словарь нужен только для CNN и LSTM моделей. BERT и DistilBERT используют свои токенизаторы.

1. В ноутбуке `cnn_training.ipynb` или `lstm_training.ipynb` найдите ячейку "Save Vocabulary"
2. Выполните эту ячейку
3. Файл `vocab.json` будет скачан в папку Downloads

### Шаг 5: Скопируйте файлы в проект

После скачивания всех файлов из Colab:

1. Откройте папку Downloads на вашем компьютере
2. Найдите файлы:
   - `best_cnn_model.pth` (из CNN ноутбука)
   - `best_lstm_model.pth` (из LSTM ноутбука)
   - `best_bert_model.zip` (из BERT ноутбука) - распакуйте перед копированием
   - `best_distilbert_model.zip` (из DistilBERT ноутбука) - распакуйте перед копированием
   - `vocab.json` (из CNN или LSTM ноутбука)

3. Скопируйте файлы в соответствующие папки проекта:

```
Fake-News-Classifier-2/
├── models/
│   ├── best_cnn_model.pth              ← скопируйте сюда
│   ├── best_lstm_model.pth             ← скопируйте сюда
│   ├── best_bert_model/                ← распакуйте zip и скопируйте папку
│   │   ├── config.json
│   │   ├── pytorch_model.bin
│   │   └── tokenizer files...
│   └── best_distilbert_model/          ← распакуйте zip и скопируйте папку
│       ├── config.json
│       ├── pytorch_model.bin
│       └── tokenizer files...
└── vocab/
    └── vocab.json                      ← скопируйте сюда (только для CNN/LSTM)
```

## ✅ Проверка готовности файлов

После копирования файлов запустите скрипт проверки:

```bash
python scripts/prepare_for_railway.py
```

Скрипт проверит:
- ✓ Наличие всех файлов и папок
- ✓ Размер файлов и папок
- ✓ Структуру словаря (для CNN/LSTM)
- ✓ Наличие необходимых файлов в папках BERT и DistilBERT моделей

Если все файлы на месте, вы увидите сообщение:
```
✓ ВСЕ ФАЙЛЫ НАЙДЕНЫ И ГОТОВЫ К ДЕПЛОЮ!
```

## 🔧 Способ 2: Локальное обучение (Альтернатива)

Если у вас есть мощный компьютер с GPU, вы можете обучить модели локально:

1. Установите зависимости:
```bash
pip install torch torchvision torchaudio scikit-learn pandas numpy matplotlib seaborn tqdm kagglehub
```

2. Запустите ноутбуки локально (используя Jupyter):
```bash
jupyter notebook notebooks/cnn_training.ipynb
jupyter notebook notebooks/lstm_training.ipynb
jupyter notebook notebooks/bert_training.ipynb
jupyter notebook notebooks/distilbert_training.ipynb
```

3. После обучения файлы будут сохранены в текущей директории:
   - CNN/LSTM: `best_*_model.pth`
   - BERT/DistilBERT: `best_*_model/` (папки)

4. Переместите их в соответствующие папки проекта

**Примечание:** 
- Для локального обучения нужно будет убрать строки с `files.download()` из ноутбуков, так как эта функция работает только в Colab
- BERT и DistilBERT модели сохраняются в папках, а не в отдельных файлах `.pth`
- Для BERT и DistilBERT не нужен словарь `vocab.json` - они используют свои токенизаторы

## 📝 Структура файлов для Railway

После подготовки файлов структура проекта должна выглядеть так:

```
Fake-News-Classifier-2/
├── backend/
│   ├── main.py
│   ├── models/
│   ├── preprocessing/
│   └── utils/
├── models/                    ← Модели должны быть здесь
│   ├── best_cnn_model.pth
│   ├── best_lstm_model.pth
│   ├── best_bert_model/       ← Папка с файлами BERT
│   └── best_distilbert_model/ ← Папка с файлами DistilBERT
├── vocab/                     ← Словарь должен быть здесь (для CNN/LSTM)
│   └── vocab.json
├── requirements.txt
├── Procfile
└── railway.json
```

## 🚢 Следующие шаги

После того как все файлы готовы:

1. Проверьте файлы: `python scripts/prepare_for_railway.py`
2. Следуйте инструкциям в `RAILWAY_DEPLOYMENT.md` для деплоя
3. Загрузите файлы на Railway через Dashboard или CLI

## ❓ Частые проблемы

### Файлы не скачиваются из Colab

- Убедитесь, что выполнили ячейку "Download Model for Railway Deployment"
- Проверьте папку Downloads на вашем компьютере
- Попробуйте скачать файлы вручную через Colab (правый клик → Download)

### Модели слишком большие для GitHub

- Модели обычно не коммитятся в Git (добавлены в .gitignore)
- Загружайте модели напрямую на Railway через Dashboard или CLI
- BERT и DistilBERT модели могут быть большими (100-400 MB), используйте zip архивы

### Словарь не найден

- Убедитесь, что выполнили ячейку "Save Vocabulary" в CNN или LSTM ноутбуке
- Проверьте, что файл `vocab.json` скопирован в папку `vocab/`
- **Примечание:** BERT и DistilBERT не требуют словаря - они используют свои токенизаторы

### BERT/DistilBERT модели не загружаются

- Убедитесь, что вы распаковали zip архив перед копированием
- Проверьте, что вся папка скопирована (не только отдельные файлы)
- Убедитесь, что в папке есть файлы: `config.json`, `pytorch_model.bin`, и файлы токенизатора

## 📞 Дополнительная помощь

Если у вас возникли проблемы:

1. Проверьте логи обучения в ноутбуках
2. Убедитесь, что все ячейки выполнены успешно
3. Проверьте размер файлов:
   - CNN/LSTM модели: ~7-10 MB каждая
   - BERT модель: ~400-500 MB (папка)
   - DistilBERT модель: ~250-300 MB (папка)
4. Запустите скрипт проверки: `python scripts/prepare_for_railway.py`

---

**Удачи с деплоем! 🚀**

