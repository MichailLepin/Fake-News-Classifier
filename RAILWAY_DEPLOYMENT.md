# Railway.app Deployment Guide

## Быстрый старт

### 1. Подготовка файлов

Перед деплоем убедитесь, что у вас есть:
- ✅ `best_lstm_model.pth` - обученная LSTM модель
- ✅ `best_cnn_model.pth` - обученная CNN модель  
- ✅ `vocab.json` - словарь из ноутбука обучения

### 2. Развертывание на Railway

1. **Зарегистрируйтесь на Railway:**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий `Fake-News-Classifier-1`

3. **Railway автоматически:**
   - Определит Python проект
   - Установит зависимости из `requirements.txt`
   - Запустит приложение используя `Procfile` или `railway.json`

4. **Настройте переменные окружения:**
   В настройках проекта (Settings → Variables) добавьте:
   ```
   MODELS_DIR=models
   VOCAB_PATH=vocab/vocab.json
   ALLOWED_ORIGINS=http://localhost:8080,https://YOUR_USERNAME.github.io
   GITHUB_PAGES_DOMAIN=https://YOUR_USERNAME.github.io
   ```
   Замените `YOUR_USERNAME` на ваш GitHub username.

5. **Загрузите файлы моделей:**
   
   **Вариант A: Через Railway Dashboard**
   - Откройте проект в Railway
   - Перейдите в раздел "Files" или используйте Railway CLI
   - Загрузите файлы в соответствующие папки:
     - `models/best_lstm_model.pth`
     - `models/best_cnn_model.pth`
     - `vocab/vocab.json`

   **Вариант B: Через Railway CLI**
   ```bash
   # Установите Railway CLI
   npm i -g @railway/cli
   
   # Войдите
   railway login
   
   # Подключитесь к проекту
   railway link
   
   # Загрузите файлы
   railway up models/best_lstm_model.pth
   railway up models/best_cnn_model.pth
   railway up vocab/vocab.json
   ```

6. **Получите URL:**
   - Railway автоматически создаст URL вида: `https://your-app.up.railway.app`
   - Скопируйте этот URL

### 3. Обновление фронтенда

1. Откройте `docs/index.html`
2. Найдите функцию `getAPIBaseURL()` (около строки 803)
3. Замените:
   ```javascript
   return 'https://YOUR_BACKEND_URL.com/api';
   ```
   На ваш Railway URL:
   ```javascript
   return 'https://your-app.up.railway.app/api';
   ```

4. Закоммитьте и запушьте изменения:
   ```bash
   git add docs/index.html
   git commit -m "Update API URL for Railway deployment"
   git push
   ```

5. GitHub Pages автоматически обновится

### 4. Проверка работы

1. Откройте ваш GitHub Pages сайт
2. Перейдите на вкладку "🤖 Model Demo"
3. Проверьте статус API внизу страницы (должен быть зеленый)
4. Попробуйте классифицировать текст

## Структура файлов для Railway

```
.
├── backend/              # FastAPI приложение
│   ├── main.py          # Главный файл приложения
│   ├── models/          # Определения моделей
│   ├── preprocessing/   # Предобработка текста
│   └── utils/           # Утилиты
├── models/              # Обученные модели (загрузить в Railway)
│   ├── best_lstm_model.pth
│   └── best_cnn_model.pth
├── vocab/               # Vocab файлы (загрузить в Railway)
│   └── vocab.json
├── requirements.txt     # Python зависимости
├── Procfile            # Команда запуска для Railway
├── railway.json        # Конфигурация Railway
└── runtime.txt         # Версия Python
```

## Переменные окружения

| Переменная | Описание | Пример значения |
|-----------|----------|-----------------|
| `MODELS_DIR` | Директория с моделями | `models` |
| `VOCAB_PATH` | Путь к vocab файлу | `vocab/vocab.json` |
| `GLOVE_PATH` | Путь к GloVe embeddings (опционально) | `vocab/glove.6B.100d.txt` |
| `ALLOWED_ORIGINS` | Разрешенные домены для CORS | `http://localhost:8080,https://username.github.io` |
| `GITHUB_PAGES_DOMAIN` | Домен GitHub Pages | `https://username.github.io` |

## Устранение проблем

### Модели не загружаются

1. Проверьте, что файлы загружены в правильные папки
2. Проверьте переменные окружения `MODELS_DIR` и `VOCAB_PATH`
3. Посмотрите логи Railway (View Logs)

### CORS ошибки

1. Убедитесь, что `GITHUB_PAGES_DOMAIN` указан правильно
2. Проверьте формат в `ALLOWED_ORIGINS` (через запятую, без пробелов)
3. Проверьте консоль браузера на ошибки

### Приложение не запускается

1. Проверьте логи Railway
2. Убедитесь, что все зависимости в `requirements.txt`
3. Проверьте, что `Procfile` или `railway.json` настроены правильно

## Мониторинг

Railway предоставляет:
- Логи в реальном времени
- Метрики использования
- История деплоев
- Переменные окружения

Доступ через Railway Dashboard или CLI.

## Обновление

После изменений в коде:
1. Закоммитьте и запушьте в GitHub
2. Railway автоматически перезапустит приложение
3. Проверьте логи на ошибки

## Стоимость

Railway предоставляет:
- **Free tier**: $5 кредитов в месяц
- **Hobby plan**: $5/месяц (больше ресурсов)
- Для этого проекта Free tier должно быть достаточно

## Дополнительные ресурсы

- [Railway Documentation](https://docs.railway.app/)
- [Railway CLI](https://docs.railway.app/develop/cli)
- [FastAPI Deployment](https://fastapi.tiangolo.com/deployment/)

